const express = requires('express')
/*const app= express()
const mongoose = requires('mongoose')*/

/*connexion à la database*/
/*mongoose.connect(process.env.DATABASE_URL, {useNewUrlParser: true })*/

/*création variable db pour connecter certains évenements à exécuter lorsque notre base de donnée est connectée, permet de savoir si ça marche correctement*/
/*const db = mongoose.connection */

/*en cas d'erreur, nous nous déconnectons
cela nous permet de voir s'il y a un problème de connexion à notre bdd*/
/*db.on('error', (error) => console.error(error))*/

/*une fois connecté, nous ouvrons la database, un msg de connexion s'affiche*/
/*db.once('open', () => console.log('Connected to Database'))*/

/*app.listen(3000, () => console.log('Server Started'))*/